# btctxbuilder (reference Go implementation)

Frozen reference for the Rust wallet in `crates/` and `apps/`. Kept for parity checks,
regression comparison and recovering intended behavior; not actively extended.

```bash
make run          # bubbletea TUI
go test ./...     # note: client tests hit blockstream.info
```
